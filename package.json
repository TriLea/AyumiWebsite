{
  "scripts": {
    "start": "parcel index.html calendar/*.html auth/login.html",
    "build": "parcel build index.html calendar/*.html auth/login.html",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^10.7.2",
    "ig": "^0.0.5",
    "npm": "^10.4.0"
  },
  "devDependencies": {
    "parcel": "^2.11.0",
    "process": "^0.11.10"
  }
}
